diff --git a/dist/utilities/use-search-params/use-search-params.svelte.js b/dist/utilities/use-search-params/use-search-params.svelte.js
index 8ddeb0eacf7ef5884c222536ff76588a4134d0c7..ad33af1fc23d327e8666757144f7340474d8cb41 100644
--- a/dist/utilities/use-search-params/use-search-params.svelte.js
+++ b/dist/utilities/use-search-params/use-search-params.svelte.js
@@ -683,7 +683,7 @@ class SearchParams {
             this.#localCache = new SvelteURLSearchParams();
             // If updateURL is true, clear the URL
             if (this.#options.updateURL && BROWSER && !building) {
-                goto("?", { replaceState: true, noScroll: this.#options.noScroll });
+                goto("?", { replace: true, reset: !this.#options.noScroll });
             }
         }
     }
@@ -786,10 +786,7 @@ class SearchParams {
             if (!BROWSER)
                 return;
             // When pushHistory is false, use replaceState to avoid creating a browser history entry
-            const gotoOptions = !this.#options.pushHistory
-                ? { replaceState: true, keepFocus: true }
-                : { keepFocus: true };
-            goto("?" + params.toString(), { ...gotoOptions, noScroll: this.#options.noScroll });
+            goto("?" + params.toString(), { replace: !this.#options.pushHistory, reset: !this.#options.noScroll });
         };
         // If debounce is set, delay the URL update
         if (this.#options.debounce && this.#options.debounce > 0) {
@@ -1395,7 +1392,7 @@ export function useSearchParams(schema, options = {}) {
                     for (const key of invalidKeys) {
                         newSearchParams.delete(String(key));
                     }
-                    goto("?" + newSearchParams.toString(), { replaceState: true });
+                    goto("?" + newSearchParams.toString(), { replace: true });
                 }
             }
         }
@@ -1443,7 +1440,7 @@ export function useSearchParams(schema, options = {}) {
                     if (needsUpdate) {
                         // Always use replaceState: true for initialization to avoid creating a new history entry
                         // Don't use debouncing for initialization as this is a one-time operation
-                        goto("?" + newSearchParams.toString(), { replaceState: true });
+                        goto("?" + newSearchParams.toString(), { replace: true });
                     }
                 }
             }
